---
# tasks file for baseline
- name: Upgrade packages
  apt: upgrade=safe
  tags: ['packages']

- name: Install baseline
  apt: name={{item}} state=present
  with_items:
    - ufw
    - git
    - mono-complete
  tags: ['packages']
