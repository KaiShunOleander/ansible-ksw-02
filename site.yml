---
# KaiShunWorldz grid conf01 files
# Date : July 20, 2018

- hosts: all
  become: yes
  gather_facts: no
  tasks:
    - name: Update apt cache
      apt:
        update_cache: true
        cache_valid_time: 3600
      when: ansible_os_family == "Debian"

- include: orchestrator.yml
- include: storage.yml
- include: db-backup.yml
- include: opensim-grid-init.yml

